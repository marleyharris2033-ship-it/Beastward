// Beastward consolidated battle interaction v1
// Individual beast placement + upgrade-panel pause behaviour.
(()=>{
const FINGER_OFFSET=72;
let selectedUid=null,dragUid=null,dragging=false,dragGhost=null,suppressClick=false;
function instance(uid){return (save.beastInstances||[]).find(x=>x&&x.uid===uid)||null}
function currentSquad(){return (save.lastInstanceLoadout||[]).filter(u=>instance(u)).slice(0,6)}
function canvas(){return document.querySelector('#gameCanvas')||document.querySelector('canvas')}
function canvasPoint(clientX,clientY,offset=false){const c=canvas();if(!c)return null;const r=c.getBoundingClientRect(),screenY=clientY-(offset?FINGER_OFFSET:0);if(clientX<r.left||clientX>r.right||screenY<r.top||screenY>r.bottom)return null;return{x:(clientX-r.left)*c.width/r.width,y:(screenY-r.top)*c.height/r.height}}
function canPlace(uid,x,y){const i=instance(uid);if(!i||!currentSquad().includes(uid)||towers.some(t=>t.instanceUid===uid))return false;const b=beasts[i.species];return gold>=b.cost&&distPath(x,y)>=55&&!blockedByScenery(x,y)&&!towers.some(t=>Math.hypot(t.x-x,t.y-y)<64)}
function place(uid,x,y){const i=instance(uid);if(!i||!canPlace(uid,x,y))return false;const b=beasts[i.species];const placed={x,y,b:battleStats(uid),instanceUid:uid,cool:0,baseCost:b.cost,spent:b.cost,powerTier:0,specialTier:0,skillTier:0,targetMode:'first'};towers.push(placed);if(running)waveParticipants.add(uid);gold-=b.cost;selectedTower=null;selectedSpecies=null;selectedUid=null;document.querySelectorAll('.tower-choice').forEach(x=>x.classList.remove('selected'));ui();renderSelectedTower();choices();return true}
function removeGhost(){dragGhost?.remove();dragGhost=null}
function rangeFor(uid){const i=instance(uid);if(!i)return 0;try{return battleStats(uid).range||beasts[i.species].range||0}catch(_){return beasts[i.species].range||0}}
function moveGhost(e){if(!dragGhost)return;const c=canvas(),r=c?.getBoundingClientRect();dragGhost.style.left=e.clientX+'px';dragGhost.style.top=(e.clientY-FINGER_OFFSET)+'px';const p=canvasPoint(e.clientX,e.clientY,true),ring=dragGhost.querySelector('.tower-drag-range');if(ring&&r){const d=rangeFor(dragUid)*2*r.width/c.width;ring.style.width=d+'px';ring.style.height=d+'px'}dragGhost.classList.toggle('valid',!!(p&&canPlace(dragUid,p.x,p.y)));dragGhost.classList.toggle('invalid',!(p&&canPlace(dragUid,p.x,p.y)))}
function beginDrag(uid,e){if(e.pointerType==='mouse'&&e.button!==0)return;dragUid=uid;dragging=false;const sx=e.clientX,sy=e.clientY;const move=ev=>{if(!dragUid)return;if(!dragging&&Math.hypot(ev.clientX-sx,ev.clientY-sy)>8){dragging=true;suppressClick=true;const i=instance(dragUid);dragGhost=document.createElement('div');dragGhost.className='tower-drag-ghost';dragGhost.innerHTML=`<span class="tower-drag-range"></span><span class="tower-drag-beast">${stageSpriteMarkup(i.species,evolutionStage(dragUid),'tower-drag-sprite')}</span>`;document.body.appendChild(dragGhost)}if(dragging){ev.preventDefault();moveGhost(ev)}};const up=ev=>{document.removeEventListener('pointermove',move,true);document.removeEventListener('pointerup',up,true);if(dragging){ev.preventDefault();ev.stopImmediatePropagation();const p=canvasPoint(ev.clientX,ev.clientY,true);if(p)place(dragUid,p.x,p.y);setTimeout(()=>suppressClick=false,0)}removeGhost();dragUid=null;dragging=false};document.addEventListener('pointermove',move,{capture:true,passive:false});document.addEventListener('pointerup',up,true)}
choices=function(){const w=$('#towerChoices');if(!w)return;w.innerHTML='';currentSquad().forEach(uid=>{const i=instance(uid),b=beasts[i.species],s=stageStats(uid),placed=towers.some(t=>t.instanceUid===uid),el=document.createElement('button');el.className='tower-choice'+(placed?' instance-used':'');el.disabled=placed;el.innerHTML=`${stageSpriteMarkup(i.species,evolutionStage(uid),'tower-list-sprite')}<div><b>${nameFor(uid)} <em>#${uid.slice(-4).toUpperCase()}</em></b><small>Lv ${i.level} • ★${i.ascension} • ${b.cost} gold${placed?' • DEPLOYED':''}</small><small>POW ${s.power} • SPD ${s.speed} • RNG ${s.range}</small></div>`;el.addEventListener('pointerdown',e=>{if(!placed)beginDrag(uid,e)});el.onclick=()=>{if(placed||suppressClick)return;selectedUid=uid;selectedSpecies=i.species;selectedTower=null;renderSelectedTower();document.querySelectorAll('.tower-choice').forEach(x=>x.classList.remove('selected'));el.classList.add('selected')};w.appendChild(el)})};
const c=canvas();if(c){c.addEventListener('pointerdown',e=>{if(!selectedUid)return;const p=canvasPoint(e.clientX,e.clientY);if(!p)return;const existing=towers.find(t=>Math.hypot(t.x-p.x,t.y-p.y)<=40);if(existing)return;if(!canPlace(selectedUid,p.x,p.y))return;e.preventDefault();e.stopImmediatePropagation();place(selectedUid,p.x,p.y)},true)}
const style=document.createElement('style');style.textContent=`.tower-choice{touch-action:none}.tower-drag-ghost{position:fixed;z-index:99999;pointer-events:none;transform:translate(-50%,-50%);width:72px;height:72px;display:grid;place-items:center;border-radius:50%;background:#13261ccc;border:2px solid #d8bc62;box-shadow:0 8px 28px #0009;opacity:.96}.tower-drag-beast{position:relative;z-index:2;display:grid;place-items:center}.tower-drag-range{position:absolute;z-index:1;left:50%;top:50%;transform:translate(-50%,-50%);border:2px solid rgba(216,188,98,.9);background:rgba(216,188,98,.12);border-radius:50%;box-shadow:0 0 10px rgba(216,188,98,.2)}.tower-drag-ghost.invalid{border-color:#d95b5b}.tower-drag-ghost.invalid .tower-drag-range{border-color:rgba(217,91,91,.9);background:rgba(217,91,91,.1)}.tower-drag-ghost.valid{border-color:#70d98a}.tower-drag-ghost.valid .tower-drag-range{border-color:rgba(112,217,138,.95);background:rgba(112,217,138,.1)}.tower-drag-ghost .tower-drag-sprite,.tower-drag-ghost img,.tower-drag-ghost svg{max-width:62px!important;max-height:62px!important;width:62px!important;height:62px!important;object-fit:contain}`;document.head.appendChild(style);
const sell=$('#sellTowerBtn');if(sell){const old=sell.onclick;sell.onclick=e=>{const r=old?.call(sell,e);setTimeout(choices,0);return r}}try{choices()}catch(e){}document.documentElement.dataset.individualPlacement='fixed-v3';
})();

(()=>{
let upgradePaused=false;
let resumeSpeed=1;
const originalRenderSelectedTower=renderSelectedTower;
renderSelectedTower=function(){const hadTower=!!(selectedTower&&towers.includes(selectedTower));if(hadTower&&!upgradePaused){resumeSpeed=speed>0?speed:(resumeSpeed||1);speed=0;upgradePaused=true}const result=originalRenderSelectedTower.apply(this,arguments);if(!selectedTower&&upgradePaused){speed=resumeSpeed||1;upgradePaused=false}return result};
const originalCloseTowerModal=closeTowerModal;
closeTowerModal=function(){const result=originalCloseTowerModal.apply(this,arguments);if(upgradePaused){speed=resumeSpeed||1;upgradePaused=false}return result};
const sellBtn=document.querySelector('#sellTowerBtn');if(sellBtn)sellBtn.addEventListener('click',()=>{if(upgradePaused&&!selectedTower){speed=resumeSpeed||1;upgradePaused=false}});
document.documentElement.dataset.upgradePause='v1';
})();